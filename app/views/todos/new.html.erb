<h1>New Todo</h1>
<p>Rails apps are so easy</p>
<%= form_for :todo, url: todos_path do |f| %>
    <p>
      <%= f.label :title %><br/>
      <%= f.text_field :title %>
    </p>

    <p>
      <%= f.label :notes %><br/>
      <%= f.text_area :notes %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>